spring.application.name=SmartClassroomERP

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/smart_classroom_erp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Dhrudipa@1432

# Hibernate settings (Auto-creates tables based on your Java code)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT settings
app.jwt.secret=${JWT_SECRET:smart-classroom-erp-local-development-secret-please-change}
app.jwt.expiration-ms=86400000
